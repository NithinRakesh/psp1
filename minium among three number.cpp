#include <stdio.h>
int main ()
{
    int a = 5, b = 7, c = 10;
 
    if (a <= b && a <= c)
        count << a << " is the smallest";
 
    else if (b <= a && b <= c)
        count << b << " is the smallest";
 
    else
        count << c << " is the smallest";
 
    return 0;
}
